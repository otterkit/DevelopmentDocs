local data is
  number : int array := [parameters]
  fib1   : int := call main()
  fib2   : int := call main()
end local

main procedure ( number : int array ) is
  
  if number[0] < 2 then
    return number[0]
  else 
    return fib1(number[0] - 1) + fib2(number[0] - 2)
  end if
  
end main
